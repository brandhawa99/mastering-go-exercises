# Chapter 1 Exercises 
1. Write a Go program that finds the sum of all of its numeric command-line arguments 
2. Write a Go program that finds the average value of all of its float command line arguments
3. Write a Go program that keeps reading integers  until it gets the word STOP as input 